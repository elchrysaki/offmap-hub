# ADR 0001: Universal Expo frontend

Status: accepted

Use one Expo Router application for responsive web, iOS, and Android. Platform navigation may differ, but routes, screens, DTOs, feature logic, and design tokens remain shared. This avoids a separate web product while preserving native interaction and accessible responsive markup.
